{
  "name": "tab-forge",
  "version": "1.0.0",
  "description": "Chrome extension for grouping tabs",
  "main": "dist/background.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "node scripts/copy-assets.js",
    "watch": "tsc --watch",
    "clean": "del-cli dist",
    "dev": "tsc --watch"
  },
  "keywords": [
    "chrome-extension",
    "tabs",
    "tab-groups"
  ],
  "author": "Tab Forge Developer",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.244",
    "del-cli": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
